# Errata

## 21/3

Admin - Phrases
Search engine should search for phrases OR tags.
Place confirmation/error text below the search bar.

Technical

Change user list to client list.

## Consent

Enable the enter key to submit.

## Login

## Technical

Add a title attribute to the clock icon to indicate it is the last access date.

Change the design of the Sign up button: make it large, green, and include text.

Fewer fields appear when editing than when creating... if no changes are made, it seems to trigger an error.

The Modify button has a "plus" (+) icon, which implies add. Find a different icon.




## Create users
Better to create clients

Mark mandatory fields.
Use fieldset

Remove diseases, dis-- set up a field of observations

## Record audios

Place the play button in the centre
and the text display button (smaller) in the bottom right-hand corner
and also include the skip button in the dialogue box

In the tag cloud, display the most common tags, along with a number indicating the number of phrases

Hide the microphone until playback or text display begins

## Improvements 23-24

Rework the side menu into a bottom menu
Place the “Add” and “Search” options at the top


## PIA LARA coordinators' meeting

The app shows how many times each category has been recorded

When searching for a tag, the search box should look within the text of the box

When accessing the site from an incognito window, a message appears in English asking you to login
